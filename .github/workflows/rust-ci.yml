name: Rust CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Setup Rust
        uses: actions/checkout@v3
      - name: Install Dependencies
        run: sudo apt-get install -y libatk1.0-dev libgdk-pixbuf2.0-dev libgdk-pixbuf-xlib-2.0-dev libgdk3.0-cil-dev
      - name: Install cargo-audit
        run: cargo install cargo-audit
      - name: Build
        run: cargo build --verbose
      - name: Test
        run: cargo test --verbose
      - name: Clippy
        run: cargo clippy --verbose -- -D warnings
      - name: Audit
        run: cargo audit
